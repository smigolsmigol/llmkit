# Direct quality tools are exact-pinned. Transitive dependencies remain visible
# to pip-audit and `pip check`; this file is not presented as a full lock.
bandit[toml]==1.9.4
build==1.2.2.post1
coverage[toml]==7.15.1
mypy==1.19.1
pip-audit==2.10.0
pre-commit==4.6.0
pytest==9.1.1
ruff==0.15.20
